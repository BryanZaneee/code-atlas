// Same name as the real npm package, in the tsconfig's own directory — the
// shape that fabricates a phantom edge if "no baseUrl declared" is ever
// treated as "baseUrl defaults to here".
export const x = 1;
