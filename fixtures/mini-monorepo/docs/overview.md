# mini-monorepo

Two services with an import chain, endpoints in both languages, and one test per
service. Deliberately small: it exists so CI has a target that does not live
outside this repository.
